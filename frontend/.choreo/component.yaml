# +required The configuration file schema version
schemaVersion: 1.2

# +optional Build specification
build:
  # Use Docker build with custom Dockerfile
  dockerfile: Dockerfile

# +optional Incoming connection details for the component
endpoints:
  # Frontend HTTP endpoint
  - name: frontend
    displayName: CycleSync Frontend
    service:
      basePath: /
      port: 3000
    type: REST
    networkVisibilities: 
      - Public

# +required Schema version
schemaVersion: 1.0

# +required Unique identifier
id: cyclesync-frontend

# +required Component name
name: cyclesync-frontend

# +optional Display name
displayName: CycleSync Frontend

# +optional Description
description: Next.js frontend application for CycleSync circular economy platform

# +required Component type
type: WebApplication

# +required Container specification
container:
  # Docker build configuration
  dockerBuild:
    dockerfilePath: Dockerfile
    dockerContext: .

# +required Endpoint configuration
endpoints:
  - name: frontend
    port: 3000
    type: HTTP
    networkVisibility: Public
